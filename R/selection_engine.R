# Selection Engine


#' @export
selection_engine <- function(...){
    # A backend to power the `forward()` and `forward()` selection
    # procedures (i.e., the parts that are common to the two).
    #
    # Args:
    #    TBD
    # Returns:
    #    TBD
    #
    return(NULL)
}
